<Window x:Class="Fahrstundenplaner.LogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="System-Protokoll" Height="550" Width="750" 
        WindowStartupLocation="CenterOwner" Background="#F4F7F9">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="80"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2C3E50">
            <Grid Margin="25,0">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="📜" FontSize="32" VerticalAlignment="Center" Margin="0,0,15,0"/>
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="System-Aktivitätslog" Foreground="White" FontSize="22" FontWeight="Bold"/>
                        <TextBlock Text="Historie aller Anmeldungen und Datenbankänderungen" Foreground="#BDC3C7" FontSize="12"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <DataGrid x:Name="LogGrid" Grid.Row="1" Margin="20" 
                  AutoGenerateColumns="False" IsReadOnly="True" 
                  Background="White" BorderThickness="1" BorderBrush="#DCDDE1"
                  RowBackground="#FFFFFF" AlternatingRowBackground="#F9F9F9"
                  GridLinesVisibility="Horizontal" HorizontalGridLinesBrush="#F1F1F1"
                  FontSize="13" CanUserSortColumns="True">

            <DataGrid.Resources>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#ECF0F1"/>
                    <Setter Property="Padding" Value="10,8"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="BorderThickness" Value="0,0,0,1"/>
                    <Setter Property="BorderBrush" Value="#DCDDE1"/>
                </Style>
            </DataGrid.Resources>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Zeitstempel" Binding="{Binding Timestamp}" Width="170"/>
                <DataGridTextColumn Header="Benutzer" Binding="{Binding Username}" Width="120">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="FontWeight" Value="SemiBold"/>
                            <Setter Property="Padding" Value="5,0"/>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Aktion" Binding="{Binding Action}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Border Grid.Row="2" Background="#ECF0F1" BorderBrush="#DCDDE1" BorderThickness="0,1,0,0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,20,0">
                <Button Content="Schließen" Click="Close_Click" IsCancel="True" Width="100" Height="30" Cursor="Hand" Background="#95A5A6" Foreground="White">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4"/>
                        </Style>
                    </Button.Resources>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>